using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace TestCryptoApiSdkProject.Exchanges.OrderBook
{
    [TestClass]
    public class OrderBook : BaseTest
    {
        [TestMethod]
        public void TestSimple()
        {
            /*var response = Manager.Exchanges.OrderBook.OrderBook();

            Assert.IsNotNull(response);
            Assert.IsTrue(string.IsNullOrEmpty(response.ErrorMessage));
            Assert.IsNotNull(response.Periods);
            Assert.IsTrue(response.Periods.Any());*/
        }
    }
}